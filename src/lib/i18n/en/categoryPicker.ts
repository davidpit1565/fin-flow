/** The category radio-grid used inside AddTransactionSheet (and anywhere
 *  else a category needs picking). Category *names* themselves are user
 *  data and are never translated here -- only the picker's own chrome. */
export const categoryPicker = {
  defaultAriaLabel: "Category",
};
